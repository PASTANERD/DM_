#include <stdio.h>

int main(){

  FILE *stream;

  stream = fopen("sorted_list.txt", "r+w");
  int *temp;
  int dump = 0;
  int str_len;
  while((str_len = fscanf("%d\n", temp+i*sizeof(int))) > 0);

  int i = 0;
  for( i = )
}
